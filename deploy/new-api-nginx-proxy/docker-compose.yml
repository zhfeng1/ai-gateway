services:
  new-api-nginx-proxy:
    image: nginx:1.27-alpine
    container_name: new-api-nginx-proxy
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
